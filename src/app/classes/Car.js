export default class Car {
  constructor(name, carrying, speed, loadingTime, costs) {
    this.name = name;
    this.carrying = carrying;
    this.speed = speed;
    this.loadingTime = loadingTime;
    this.costs = costs;
  }
}